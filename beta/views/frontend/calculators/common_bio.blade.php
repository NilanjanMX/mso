
@if($calculater_heading)
    @if($calculater_heading->boi)
        <div class="card-header" style="margin-bottom: 15px;">
            {!! $calculater_heading->boi !!}
        </div>
    @endif
@endif